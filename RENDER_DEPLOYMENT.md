# دليل الرفع على منصة Render

تهانينا! مشروعك الآن مهيأ بالكامل للعمل على خوادم **Render** السحابية بشكل مجاني ومستقر. 
لقد قمنا بإضافة ملف `render.yaml` الذي سيقوم بإعداد كل شيء (قاعدة البيانات + الخادم + المتغيرات البيئية) بضغطة زر واحدة.

اتبع هذه الخطوات البسيطة لنشر موقعك على الإنترنت:

### الخطوة الأولى: رفع الكود إلى GitHub
بما أن مشروعك يحتوي بالفعل على مجلد `.git`، اتبع هذه الأوامر في الـ Terminal لرفع الكود إلى مستودعك (تأكد من إنشاء مستودع Repository فارغ على GitHub أولاً):

```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```
*(إذا لم تكن قد ربطت حساب GitHub، استخدم الأمر `git remote add origin <URL>` الذي يعطيك إياه GitHub).*

### الخطوة الثانية: الاتصال بـ Render
1. اذهب إلى موقع [Render.com](https://render.com/) وقم بتسجيل الدخول باستخدام حسابك في GitHub.
2. من لوحة التحكم (Dashboard)، اضغط على الزر الأزرق **"New"**.
3. من القائمة المنسدلة، اختر **"Blueprint"** (هذا الخيار هو الذي سيقرأ ملف `render.yaml` الخاص بنا).

### الخطوة الثالثة: ربط المشروع
1. ستظهر لك قائمة بمستودعاتك (Repositories) على GitHub، ابحث عن المستودع الذي رفعنا إليه المشروع واضغط على **Connect**.
2. سيقوم Render تلقائياً بقراءة ملف `render.yaml`. ستظهر لك نافذة لتأكيد إنشاء "safakat-db" (قاعدة البيانات) و "safakat-web" (خدمة الويب).
3. اضغط على الزر أسفل الصفحة للموافقة (Apply / Create Resources).

### الخطوة الرابعة: الاسترخاء ومراقبة البناء ☕
- سيقوم Render تلقائياً بالخطوات التالية:
  - إنشاء قاعدة بيانات PostgreSQL وربطها بالخادم.
  - تشغيل ملف `build.sh` (والذي يقوم بتثبيت المكتبات، جمع ملفات static، وعمل migrations).
  - تشغيل خادم `gunicorn` لبدء المنصة.
- ستأخذ العملية من دقيقتين إلى 5 دقائق. بمجرد ظهور كلمة **Live** باللون الأخضر، سيكون موقعك جاهزاً ومتاحاً على رابط مثل `https://safakat-web.onrender.com`.

### ملاحظات هامة:
- ملف `render.yaml` قام بتوليد `SECRET_KEY` سري وأوتوماتيكي خصيصاً لك.
- وضعنا `DEBUG = False` لضمان الحماية في بيئة الإنتاج (Production).
- تم إعداد قاعدة البيانات لتعمل تلقائياً دون الحاجة لكتابة بياناتها يدوياً.
- عند قيامك بأي تحديث مستقبلي ورفعه إلى GitHub (`git push`)، سيقوم Render بإعادة بناء المنصة وتحديثها أوتوماتيكياً!
